apiVersion: v1
kind: Service
metadata:
  name: platform-ui
  labels:
    app: platform-ui
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: platform-ui