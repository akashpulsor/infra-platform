apiVersion: v1
kind: Service
metadata:
  name: dashboard-ui
  labels:
    app: dashboard-ui
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: dashboard-ui