apiVersion: v1
kind: Service
metadata:
  name: creative-worker-ui
  labels:
    app: creative-worker-ui
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: creative-worker-ui
