{{- if .Values.secretEnv }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "admin.fullname" . }}-secrets
  labels: {{- include "admin.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $k, $v := .Values.secretEnv }}
  {{ $k }}: "{{ $v }}"
{{- end }}
{{- end }}
